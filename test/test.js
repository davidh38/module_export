var assert = require('assert');
var mine = require('../mine.js')
describe('mine.js', function() {
  describe('#favoriteBook()', function() {
    it('value has to be 5', function() {
      assert.equal(book.favoriteBook(),5);
    });
  });
});
